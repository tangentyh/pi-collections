# themes/

Drop theme JSON files here (see [pi themes docs](https://github.com/earendil-works/pi-coding-agent/blob/main/docs/themes.md)), then:

```bash
./install.sh themes
```

Select with `/settings` or `"theme": "name"` in `~/.pi/agent/settings.json`.
